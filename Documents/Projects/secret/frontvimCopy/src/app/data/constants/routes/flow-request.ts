let mainPoint = 'programas';
let secondaryPoint = 'solicitud';

export const FlowRequestsRoutes = {
    register: `${mainPoint}/${secondaryPoint}/registro`,
    citizen: `${mainPoint}/${secondaryPoint}/ficha`,
    edit:  `${mainPoint}/${secondaryPoint}/editar`
}